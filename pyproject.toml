[project]
name = "explaind"
version = "0.5.0"
description = "Debugging assistant powered by Gemma 4"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ollama",
]

[project.scripts]
explaind = "explaind.cli:main"
