[project]
name = "explaind"
version = "0.5.1"
description = "Local CLI for steering Gemma 4 reasoning using structured prompt biases and ability-based cognitive control."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ollama",
]

[project.scripts]
explaind = "explaind.cli:main"
