[project]
name = "explaind"
version = "0.4.2"
description = "Local-first debugging CLI powered by Gemma 4"
readme = "README.md"
requires-python = ">=3.11"

[project.scripts]
explaind = "explaind.cli:main"